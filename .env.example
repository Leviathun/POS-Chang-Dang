# Server Configuration
PORT=3000
APP_ENV=development

# 🗄️ Cloud SQL Database (Turso DB)
# Production DB:  libsql://changdang-pos-leviathun.aws-ap-northeast-1.turso.io
# Staging DB:     libsql://changdang-pos-staging-your-account.turso.io
# (Leave empty to use local SQLite database fallback)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

